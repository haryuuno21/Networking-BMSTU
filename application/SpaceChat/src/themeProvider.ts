import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: "#335E6F",
      },
      secondary: {
        main: "#ffffff",
      },
      background: {
        default: "#f5f5f5",
        paper: "#ffffff",
      },
    },
  });

export default theme;